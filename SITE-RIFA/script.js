let res = window.document.getElementById('caixabilhete')

function adicionar1(init) {
    var count = init || 0
    for (let c = 0; count < in) {
        count++;
        res.innerHTML = (count)
    }
}

function adicionar30() {
    res.innerHTML = '30' 
    
     
}